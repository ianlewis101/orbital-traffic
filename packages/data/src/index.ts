/**
 * @orbital/data — typed access layer over the static snapshots served from
 * the app's `public/` directory. Pure fetch + types; no framework coupling.
 */
export * from './types.js';
export * from './client.js';
