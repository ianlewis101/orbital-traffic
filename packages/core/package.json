{
  "name": "@orbital/core",
  "version": "2.0.0",
  "description": "Framework-agnostic orbital-mechanics + classification engine. Pure TypeScript, zero UI dependencies — shared by the web app, the edge worker, and any future native client.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {
    "satellite.js": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
