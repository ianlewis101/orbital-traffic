{
  "name": "@orbital/edge",
  "version": "2.0.0",
  "description": "Cloudflare Worker that proxies + caches CelesTrak / crew / ISS feeds for the client. Reuses @orbital/core's classification — no more triplicated logic.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint src"
  },
  "dependencies": {
    "@orbital/core": "workspace:*",
    "hono": "^4.6.16"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.99.0"
  }
}
