{
  "name": "@orbital/data",
  "version": "2.0.0",
  "description": "Typed, framework-agnostic data-access layer for the orbital snapshots (catalog, NEOs, descriptions, imagery, ISS feed).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@orbital/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
