# Orbital Traffic — Audit Status Tracker

**Source of truth:** this file, committed in the repo at `docs/audit-status.md`.
A copy is also kept as a project file here in Claude for quick reference —
if the two ever disagree, the repo copy wins.

**How this works:** Claude updates this file's content as work happens and
mentions it briefly — no need to ask permission each time. Actually syncing
a new version into the repo (or re-uploading here) still needs a deliberate
step, since Claude has no write access to GitHub.

**Last synced:** 2026-08-17 (second pre-submission App Store audit — found four
non-station objects sitting in `STATION_CORE_IDS`, one of them live in
production rendering as a space station and carrying a fabricated ISS-module
description; replaced the stock Capacitor launch screen; added enforcement for
the object-count convention. Full detail in the change log below. All six Worker
endpoints re-verified live again, all green.)

**Previous sync:** 2026-08-11 (pre-submission App Store audit — photo
provenance, the "COOL SHIT" category removal, the iOS version mismatch, an
esc() gap in figures.js, and a full resync of this file: every row below that
still said "Fixed, not deployed" had in fact merged, and all six Worker
endpoints were re-verified live against production, including /satcat which was
still 404ing at the previous sync)

**Status values:** Not started · In progress · Fixed, not deployed · Fixed
and live · Won't fix · N/A (audit itself was corrected, no code issue)

---

## 📦 iOS build batch tracker

**Rule:** trigger a new internal TestFlight build (via the `iOS Build &
Upload` GitHub Actions workflow, `workflow_dispatch`, no Xcode needed) once
3–5 merged-but-undeployed fixes have piled up. Ian is currently the only
tester, so there's no urgency to ship a build for any single fix alone.

**Known extra step after every build — RESOLVED 2026-08-11:** builds used to
land in "Missing Compliance" in App Store Connect → TestFlight → iOS Builds and
not reach testers until someone clicked "Manage" and answered the
export-compliance question. `ITSAppUsesNonExemptEncryption` is now set to
`false` directly in `Info.plist`, so App Store Connect stops asking and the
manual click is no longer needed. Left documented here because this caused the
"it didn't make it" confusion on 2026-07-13 — if a future build ever shows
"Missing Compliance" again, check that key first.

**⚠️ A NEW BUILD IS REQUIRED BEFORE APP STORE SUBMISSION.** The pre-submission
audit bumped `MARKETING_VERSION` from 1.0 to 2.0.0, and a marketing version
only takes effect in a freshly archived build. Everything in the table below
is already live on orbitaltraffic.app; none of it is on a device until a build
is cut.

**Merged, not yet in an iOS build:**

| # | Item | PR | Merged date |
|---|---|---|---|
| F4 | SGP4 wasted on hidden objects | #79 | 2026-07-13 |
| F5 | Boot ingest blocks main thread | #80 | 2026-07-13 |
| F6 | Time-machine × selection perf cliff | #81 | 2026-07-13 |
| — | Everything merged 2026-07-14 → 2026-08-11 | various | see change log |
| — | App Store audit: photo provenance, `cool` category removal, `MARKETING_VERSION` 2.0.0, `esc()` in figures.js | #165 | 2026-08-11 |
| — | Second App Store audit: `STATION_CORE_IDS` cleanup, fabricated ISS-module descriptions removed, real launch screen, object-count guard | this PR | 2026-08-17 |

**Last build cut:** 2026-07-13 — via the "iOS Build & Upload" GitHub Actions
workflow, after PR #78 fixed the recurring build-number collision
(`CURRENT_PROJECT_VERSION` was hardcoded to 1; now auto-increments via
`github.run_number`). This build carried F16, F7, F3, F1, and F2.
Confirmed installed and running via TestFlight. **Everything merged since that
date — nearly four weeks of work — is still not on a device.**

---

## Design & Polish (18 items — see audit §2; #18 found during later work, not in the original audit)

| # | Item | Status | PR / Branch | Notes |
|---|---|---|---|---|
| 1 | Visible data-freshness indicator | Not started | — | |
| 2 | Remove fabricated crew data | Not started | — | Same underlying issue as Finding F2 |
| 3 | Single source of truth for object count | **Fixed and live** | PR #121 (`claude/orbital-object-count-sync-d8lc29`), merged | 2026-07-22 — splash-screen figure (`#splash-msg`) is now derived at build time in `apps/web/vite.config.js` from the real length of `apps/web/public/data/satellites.json` (18,762 records as of this fix → rounds down to "18,000+"), injected via Vite `define` as `__OBJECT_COUNT__` and set by `main.js` at boot; the static HTML fallback no longer hand-writes a number. README, manifest.json, and docs/archive/store-metadata.md (App Store description, two mentions) were the hand-written surfaces that couldn't be templated — all three updated to 18,000+ and added to a new Conventions checklist in CLAUDE.md so they get updated together next time. Repo-wide grep confirmed no other stale "11,000"/"15,000"/"18,000" mentions remained. |
| 4 | Accessibility floor (focus, buttons, motion, type size) | Not started | — | |
| 5 | km/mi units toggle | **Fixed and live** | branch `claude/new-session-12sdbb`, merged | 2026-07-29 — shipped as part of the Settings panel. New `apps/web/src/util/units.js` centralises the conversions that were previously a hardcoded `MI = 0.621371` inlined at three points in `ui/info.js`; Settings → Display drives it, and the object card repaints immediately on change. Scoped to *formatted measurements* — the object card's telemetry grid and the new overhead list. `ui/describe.js`'s hand-written prose ("340 miles up", "22,236 miles up") is deliberately left imperial: those are curated sentences, not formatted numbers, and making them unit-aware means rewriting copy rather than swapping a formatter. Default stays imperial, so no existing user's view changes unless they opt in — the "metric default?" Open Question below is still open and is now a one-line change if Ian wants it flipped. The five `units.js` exports are enumerated in `eslint-rules/no-unescaped-innerhtml.js`'s `SAFE_FUNCTIONS` (each returns a localized number plus a fixed unit word, never an HTML metacharacter — pinned by a test). The now-dead `fmt()` helper in `info.js` was removed, and `"fmt"` was dropped from `SAFE_FUNCTIONS` with it: leaving an allowlisted name with no implementation would silently pre-approve any future function that happened to reuse it. |
| 6 | Rename mismatched color tokens | Not started | — | |
| 7 | Type/spacing scale | Not started | — | |
| 8 | Loading/empty/error state system | Not started | — | |
| 9 | Starlink/Kuiper duplicate color | **Fixed and live** | PR #92 (`claude/oneweb-kuiper-categories-g6g4uz`), merged | Fixed alongside F10, 2026-07-15 — kuiper's color changed from 0x8fd6ff (duplicate of starlink) to 0xa3e635; oneweb also gets its own distinct color (0x3d8bfd) as part of the same change |
| 10 | Motion system consistency | Not started | — | |
| 11 | CSS grid instead of hand-measured positions | Not started | — | |
| 12 | Decide mobile Time Machine / close-button parity | Not started | — | Blocked on Open Question: was this intentional? |
| 13 | Self-host fonts | **Fixed and live** | PR #143 (`claude/self-host-google-fonts-szv1c0`), merged | 2026-07-30 — confirmed all three fonts (Bricolage Grotesque, Oxanium, Orbitron) are SIL OFL 1.1 before bundling (license text saved alongside each font, `apps/web/public/fonts/OFL-*.txt`); OFL explicitly permits embedding/redistribution, just not selling the font by itself. Downloaded only the latin-subset woff2 for the weights the Google Fonts URL actually requested (Bricolage 500/700/800, Oxanium 400/500/600/700, Orbitron 700/800/900) — a repo-wide check confirmed every string this app renders (satellite/COSPAR names, the `OWNERS` list in `ui/info.js`) is plain ASCII, so latin-ext/vietnamese would have added weight with nothing to display. Each family serves from one physical variable-font file reused across its pinned weights in `app.css`'s new `@font-face` block, matching exactly what Google's own CSS served (same url per weight within a subset). Preserved the `unicode-range` exactly, which matters beyond bytes: the `#info .flagline` comment in `app.css` documents that Oxanium's latin-subset range excluding the flag-emoji codepoint block is what keeps the emoji-font fallback working on desktop, and self-hosting had to not regress that. Removed the `<link>`/`preconnect` tags from `index.html`. Verified in a real Chromium session (both 430×932 and 1440×900) via `document.fonts` that all three families load from `/fonts/*.woff2` with zero requests to fonts.googleapis.com/fonts.gstatic.com, and visually confirmed the wordmark (Orbitron), body/telemetry text (Oxanium), and the info-card object name + Settings section headers (Bricolage Grotesque) all render as before. Also closes the Google Fonts privacy gap (see F16) — privacy.html's "Fonts" paragraph, which named Google as a third party receiving visitor IPs, no longer applies and was removed. |
| 14 | Standardize icons | Not started | — | |
| 15 | Tablet breakpoint | Not started | — | |
| 16 | Rework "Catalogued. Not curated." veil | **Fixed and live** | PR #104 (`fix/science-allowlist-and-veil-trigger`), merged | 2026-07-17 — veil now triggers on whether an `other`-category object has a curated description (matching the lookup already used in `setFlagLine`/`describe()`) instead of `s.cat === "other"` alone; fixes ~142 already-described `other` objects (GAOFEN, SkySat, CORAL, etc.) that were wrongly veiled. Also moves 20 individually-verified objects (AJISAI, AURA, BIOMASS, ETALON 1/2, GOSAT/GOSAT-GW, GPM-CORE, ICESAT-2, JASON-3, LAGEOS 1/2, LARES/LARES-2, LARETS, MICROCARB, SMOS, STARLETTE, STELLA, SUOMI NPP) from `other` to `SCIENCE_IDS`. **Correction, same day:** the first version of this fix checked `descs[id].d` across every category, not just `other` — real-catalog verification (triggered by Crew Dragon 12 unexpectedly showing veiled) found that would have newly veiled 15,161 non-`other` objects that were never veiled before, including the ISS and all of Starlink/OneWeb/Kuiper, because those rely on `describe.js`'s generic name-pattern/`classify()` fallback text rather than a per-object `descriptions.json` entry. The veil check is now scoped to `cat === "other"` only. Client-side only (`packages/catalog` + `apps/web`) — no Worker changes, auto-deploys to web on merge. |
| 17 | Use LL2's per-person role data for commander detection | **Fixed and live** | PR #131 (`feat/astronaut-bios`), merged | 2026-07-24 — done as part of the astronaut-profile work, which had to plumb `role` through `/crew` anyway. Confirmed the suspicion recorded when this item was filed: the Worker never populated `p.role`, so `crew.js`'s `i === 0 \|\| (p.role \|\| "").toLowerCase().includes("commander")` had *always* been falling through to "whoever LL2 listed first" — the role half of that check was dead code. `buildCrew()` now sends each member's `crew[].role.role`, and the highlight prefers it, keeping the positional rule as an explicit fallback for a roster served from an edge cache filled before roles existed. Needs the same Worker deploy as the rest of PR #131. |
| 18 | iOS splash screen is still the stock Capacitor placeholder | **Fixed, not deployed** (needs the next iOS build) | this PR | 2026-08-17 — regenerated from `tools/assets/app-icon.svg` via `npm run icons:generate`, which now emits the launch image alongside the icons instead of leaving it as the one hand-maintained raster. Two changes, matching the two problems this row recorded: the artwork is the Orbital Traffic spacecraft rather than Capacitor's blue chevron, and the field is the app's own `#07080f` instead of white, so there is no bright flash before the UI paints. `LaunchScreen.storyboard`'s image-view background was also switched off `systemBackgroundColor` (white in light mode) to the same colour — the storyboard was a second, separate source of the white flash and changing only the PNG would have left it. **The safe zone was the real constraint, and it is now enforced rather than eyeballed:** iOS scales this square with `scaleAspectFill`, so on a 1320x2868 iPhone 16 Pro Max only the middle ~46% of its width is ever on screen. The craft at the icon's standard 1.04 scale spans ~87% of the canvas and would have been sliced through both solar arrays; the splash variant uses 0.46, and a new `assertSplashSafeZone()` decodes the rendered PNG, measures the craft's actual pixel extent and fails the build if it ever leaves that strip. Verified visually at full-square, iPhone and iPad-portrait crops. Every other icon regenerated byte-identically, confirming the generator stayed deterministic. |

## Architecture (8 items — see audit §3)

| # | Item | Status | PR / Branch | Notes |
|---|---|---|---|---|
| — | Worker classification claim | **N/A — audit corrected, not a code fix** | — | Audit doc itself was wrong (2026-07-13 correction); the code was already right. Nothing to fix. |
| 1 | Event/store layer for UI modules | Not started | — | |
| 2 | Central escape helper for innerHTML sinks | Not started | — | Same root cause as Finding F12 |
| 3 | Move scheduled data to Worker Cron + KV | Not started | — | |
| 4 | Automatic Worker deploy workflow | **Fixed and live** | PR #112 (`feat/automatic-worker-deploy`), merged | High-value — would have prevented the PR #52/#53 manual-deploy incident. `.github/workflows/deploy-worker.yml` now triggers on push to main touching `worker/**` or `packages/catalog/**` (plus manual `workflow_dispatch`), runs the Worker's test suite (`npm test -w worker`) as a pre-deploy gate, then deploys via `cloudflare/wrangler-action`. `concurrency` (non-cancelling) prevents overlapping deploy runs if merges land in quick succession — same spirit as the F13 push-collision fix, applied as non-overlap instead of retry since there's no git push here to rebase against. No deploy step applies to the workflow file itself — GitHub Actions workflow files take effect on merge to main, same as F13. Closes the "Worker deploys are manual, easy to forget" pain point referenced throughout this file's change log — but does **not** retroactively deploy anything already merged: PRs #107, #108, #109's Worker-relevant parts, and #111 in particular are still pending an actual Worker deploy as of this writing and need one, whether manual or via this workflow's new `workflow_dispatch` trigger, to actually go live. |
| 5 | Staging environment | Not started | — | |
| 6 | Centralize scattered config/counts | Not started | — | |
| 7 | TypeScript/checkJs on catalog + worker | Not started | — | |
| 8 | Data-integrity safety nets (schema validation, ingest tests) | Not started | — | |

## Bugs — Critical / High (F1–F7)

| # | Finding | Status | PR / Branch | Notes |
|---|---|---|---|---|
| F1 | Crew HTTP → innerHTML XSS | **Fixed and live** | PR #76, merged; Worker deployed 2026-07-13; in TestFlight build | Live, exploitable, hit by every visitor who taps ISS |
| F1b | CREW_URL regression: switched to https://, but Open Notify's HTTPS endpoint doesn't respond | **Fixed and live** | PR #108 (`fix/revert-crew-http`), merged | Not one of the original 37 findings — a regression introduced while fixing F1. Commit e0780e9 (2026-07-13) switched CREW_URL from http:// to https://, believing Open Notify supported HTTPS. Verified directly 2026-07-20 (not assumed): `https://api.open-notify.org/astros.json` returns a connection error (refused/reset), while `http://api.open-notify.org/astros.json` returns 200 with valid data. As a result the live `/crew` endpoint had been silently returning `ok:false` with an empty roster to every visitor for about a week, undetected until now. Reverted CREW_URL to http://, with a code comment above the constant warning not to switch back to https:// without first confirming Open Notify's HTTPS endpoint actually works. F1's original XSS fix (esc()-escaping every p.name-derived value in apps/web/src/ui/crew.js) is unaffected and remains in place — this regression was purely the URL-scheme half of that commit. Needs a Worker deploy per Critical Rule #1 to go live. |
| F2 | Fabricated expedition data | **Fixed and live** | PR #77, merged; in TestFlight build | |
| F3 | Crew fetch no stale-selection guard | **Fixed and live** | PR #75, merged; in TestFlight build | |
| F4 | SGP4 wasted on hidden objects | **Fixed and live on web** (not yet in an iOS build) | PR #79 (`fix/skip-hidden-category-propagation`), merged | Client-side only — auto-deployed to orbitaltraffic.app on merge, no Worker involved |
| F5 | Boot ingest blocks main thread | **Fixed and live on web** (not yet in an iOS build) | PR #80 (`fix/chunk-boot-ingest`), merged | Client-side only — auto-deployed to orbitaltraffic.app on merge, no Worker involved |
| F6 | Time-machine × selection perf cliff | **Fixed and live on web** (not yet in an iOS build) | PR #81 (`fix/time-machine-perf-cliff`), merged | Client-side only — auto-deployed to orbitaltraffic.app on merge, no Worker involved |
| F7 | Pass-alerts toggle can get stuck | **N/A — feature removed** | PR #74 (original fix), merged; superseded by `claude/remove-pass-alerts-toggle-j3txc6` | Moot as of 2026-07-28 — the Pass Alerts toggle (and the whole feature) was removed entirely per Ian's decision on the Open Questions row below. |

## Bugs — Medium (F8–F18)

| # | Finding | Status | PR / Branch | Notes |
|---|---|---|---|---|
| F8 | Fallback merge-priority inversion | **Fixed and live** | PR #82 (`fix/fallback-merge-priority`), merged | Web-app only, no Worker change — will auto-deploy to orbitaltraffic.app on merge |
| F9 | OneWeb labeled Starlink | **Fixed and live** | PR #92, merged; Worker deployed 2026-07-16 | Fixed 2026-07-15. groups.js now tags GROUP=oneweb records "oneweb" (was "starlink"); classify.js's new correctStarlinkCat() rescues by name any record still tagged "starlink" (stale bundled satellites.json, or any fetch that predates this fix), so the fix doesn't wait on a data refresh. info.js's inferOwner() now resolves OneWeb to GBR/"OneWeb (Eutelsat)" instead of "United States" — the task spec's literal regex (`/ STARLINK \| ONEWEB /`, space-padded) turned out to silently match zero real objects against hyphenated real names ("ONEWEB-0012"), the same bug class as F36, so it's matched via normalizeVehicleName()+\b instead. Verified against real catalog data: 651/651 OneWeb-named objects → oneweb category, correct ownership. Merging auto-deploys the web app (GitHub Pages); client-side ingest() re-running categorize() on every load means the fix is effective even against currently-stale bundled/cached data. **Worker deploy confirmed 2026-07-16** via the new deploy-worker skill: live Worker bundle's group-mapping table now reads `["oneweb", "oneweb"]` (was `["oneweb", "starlink"]`), and `correctStarlinkCat()`/`ONEWEB_NAME_RE` are present in the deployed source — checked directly via the Cloudflare MCP connector's worker-code read, not just an endpoint curl. |
| F10 | Kuiper category unreachable | **Fixed and live** | PR #92, merged; Worker deployed 2026-07-16 | Fixed 2026-07-15 — classify.js gets a KUIPER_NAME_RE rescue in correctOtherCat(), same mechanism already used for other constellations with no dedicated CelesTrak group. Verified: 393/393 real Kuiper-named objects → kuiper category. **Worker deploy confirmed 2026-07-16**: live bundle now has `KUIPER_NAME_RE = /KUIPER/` wired into `correctOtherCat()`, returning `"kuiper"` — previously "kuiper" appeared only once in the entire live bundle (the bare category enum) with nothing ever assigning it, exactly matching this finding's original "unreachable" description. |
| — | ISS Unity/Zvezda/Destiny missing from station allowlist | **Fixed and live** | PR #94, merged; Worker deployed 2026-07-16 | Not one of the original 37 findings — discovered 2026-07-16 during PR #93's (capsules-category-split) real-catalog verification, flagged there but not fixed since STATION_CORE_IDS was out of scope for that PR. STATION_CORE_IDS was missing all three real NORAD IDs (25575 Unity, 26400 Zvezda, 26700 Destiny), so all three resolved to cat:"other" (hidden by default). Adding the IDs alone didn't fix it against real data: correctStationCat() only consults STATION_CORE_IDS when a record already arrives tagged "stations", but these three arrive tagged "other" in the real CelesTrak feed today. correctOtherCat() needed a matching STATION_CORE_IDS promotion check too (mirroring its existing isStationVehicle() promotion). Verified against real catalog data: all three go from cat:"other" to cat:"stations". Also surfaced, unrelated and left untouched: one existing STATION_CORE_IDS entry (37224) doesn't match any ISS module in the current bundled catalog (collides harmlessly with an unrelated classified satellite's NORAD ID); three others (27386, 28654, 37820) have no matching record in the current bundled catalog at all — worth a follow-up look at STATION_CORE_IDS's remaining completeness/accuracy whenever convenient. **Worker deploy confirmed 2026-07-16**: live `STATION_CORE_IDS` set now includes 25575/26400/26700, and `correctOtherCat()`'s matching promotion check is present in the deployed source. Also confirmed the capsules-category-split (PR #93, merged, no dedicated finding row) deployed in the same push: live `CATEGORY_IDS` now includes `"capsules"`, and both `correctStationCat()` and `correctOtherCat()` promote `isStationVehicle()` matches to `"capsules"` rather than the old `"stations"`. |
| F11b | Decayed objects never pruned | **Fixed and live** | Pre-existing (predates this conversation's work) | Verified 2026-07-13: ingest.js's epoch-guard prune (10-day threshold) correctly protects against the "partial fallback" risk the audit flagged — confirmed via dedicated tests in ingest-prune.test.js. No further work needed. |
| F11c | Capsule-status wipes history on corrupt file | **Fixed and live** | PR #99 (`fix/capsule-status-corruption-handling`), merged | tools/update-capsule-status.mjs's loadExisting() previously caught every readFile/JSON.parse error uniformly and returned null, so isFirstRun couldn't distinguish a genuinely missing capsule-status.json (real first run) from one that's corrupted/truncated/unreadable — the latter silently wiped the docking-history log and events array. Now only ENOENT (file doesn't exist) returns null; any other read error or JSON.parse failure calls process.exit(1) with a clear message, matching the precedent already set by the CelesTrak-fetch failure path in the same file. Added tools/test/update-capsule-status.test.js covering both the missing-file (isFirstRun stays true) and corrupted-file (aborts, doesn't reset) cases, plus a non-ENOENT read-error case (EISDIR). No Worker/Pages deploy applies — this is a scheduled-tool script, not shipped code; takes effect on merge for the next scheduled run of update-capsule-status.yml. |
| — | Open Notify roster ~18 months stale | **Fixed and live** (this specific stopgap; superseded as primary safeguard — see the LL2 replacement row below) | PR #109, merged | Not one of the original 37 findings. Verified directly against NASA's current expedition rosters and Wikipedia (not assumed): Open Notify's astros.json was serving a crew roster roughly 18 months out of date. Headcounts happened to look plausible even though the specific names were stale, so neither F1b's ok:false handling nor F3's stale-selection guard could have caught it — this incident was invisible to every existing safeguard. Added a partial stopgap, not a full fix: `fetchAndRenderCrew()` (apps/web/src/ui/crew.js) compares the crew fetch's headcount against how many seats are actually docked at the selected station right now, via `state.capsulesData` (populated every live sync by live.js's `fetchCapsuleStatus()`) and a per-family seat table, `CREW_SEATS_BY_FAMILY` (packages/catalog/src/classify.js: soyuz 3, dragon 4, shenzhou 3 — families absent from the map are treated as unknown/unverifiable, never guessed at zero). Flags only gross mismatches (no vehicle docked yet crew reported; crew reported as zero despite a docked vehicle; an undercount beyond a 1-person tolerance) as a small "Roster may not reflect the current crew" caveat under the still-fully-rendered roster — never flags an overcount (handover overlaps between two docked vehicles are normal) and never flags purely because a docked vehicle's family isn't in the seat table. **This check explicitly would NOT have caught the original incident** — the stale roster's headcount was plausible, only the names were wrong, which this check has no way to verify (capsule-status.json carries vehicle telemetry, never astronaut names). **Update 2026-07-21:** Open Notify itself has now been replaced entirely by Launch Library 2 (see the new row below) — this check is generic (not Open-Notify-specific) and stays in place harmlessly as a backstop, but is no longer the primary safeguard against a bad roster now that the source itself is accurate. |
| — | Open Notify replaced by Launch Library 2 (LL2) | **Fixed and live** | PR #111 (`feat/replace-crew-source-with-ll2`), merged | Not one of the original 37 findings — the actual fix the row above's stopgap was standing in for. `worker/src/index.js`'s `buildCrew()` now fetches both stations' real active-expedition crew directly from LL2 (`ll.thespacedevs.com/2.2.0/spacestation/{id}/` — production tier; `lldev.thespacedevs.com` is dev-only and must never be used) instead of Open Notify, which is no longer referenced anywhere. Station IDs verified directly against real crew rosters (cross-referenced against NASA, Wikipedia, and Xinhua): `ISS_STATION_ID = 4`, `TIANGONG_STATION_ID = 18` (not 7/8, the de-orbited Tiangong 1/2). Verified for real against the live LL2 endpoints (not just mocks): confirmed the real response shape (`active_expeditions[0].crew[].astronaut.name`) matches what the code expects, and confirmed no rate-limit headers (`X-RateLimit-*`, `Retry-After`, etc.) are present on any of the three endpoints hit per cache-fill — three requests per hourly `CREW_TTL` cache-fill is very likely fine regardless, but checked rather than assumed, the same way the earlier http-vs-https assumption about this same upstream turned out to be wrong. Partial-failure behavior is deliberate: one station's fetch failing doesn't invalidate the other's real data (`ok` is true if either station succeeds); only both failing degrades to the old empty-roster contract. **New supplementary check, `possiblyIncomplete`:** compares how many people were actually placed at a station against LL2's own `/astronaut/?in_space=true&is_human=true` count of everyone currently in space — a coarse, honest signal (same spirit as the plausibility check above, but against a different, LL2-native source) that deliberately does NOT attempt to identify who the extra person/people are or which station they belong to (that would mean cross-referencing mission/flight data, out of scope for this fix). Verified this matters in practice: LL2's per-station data was found to lag the just-docked Soyuz MS-29 crew by several days as of 2026-07-21 (13 people LL2 knows are in space vs. 10 placed at a station), which `possiblyIncomplete` correctly flags. Rendered in `crew.js` as its own small caveat ("There may be additional crew not reflected yet"), independent of and stackable with the row-above's caveat — they are not mutually exclusive. Needs a Worker deploy per Critical Rule #1 to go live. |
| F11 | GSAT attributed to ESA | **Fixed and live** | PR #89 (`fix/gsat-owner-attribution`), merged | Correct and matches the finding, but verified against real current catalog data to have no visible effect today — real GSAT satellites already get curated "ISRO" descriptions that take priority, and real Galileo satellites' names don't match either the old or new pattern due to the F36 tokenization issue. Defensive fix for future/uncurated data, not a visible behavior change right now. |
| F12 | Systemic unescaped-innerHTML (9 sinks) | **Fixed and live** | PR #83 (`fix/escape-remaining-innerhtml-sinks`) + PR #84 (`feat/lint-unescaped-innerhtml`, includes an additional real gap found via the new lint rule: `s.ownerName` in info.js's chips row) | Web-app only — auto-deploys to orbitaltraffic.app on merge. Also added a permanent custom ESLint rule (with a same-scope const-resolver) preventing future unescaped innerHTML/attribute interpolations, not just a one-time fix. |
| F13 | 12:00 UTC workflow collision | **Fixed and live** | PR #98 (`claude/workflow-schedule-collision-u642b9`), merged | Collision was actually daily, not occasional, once update-capsule-status.yml went hourly: both it and update-iss-today.yml checkout main, commit different files, and `git push` with no retry, so every day at 12:00 UTC one push could lose the race and fail silently. Fix: both workflows now `git pull --rebase origin main` before `git push`, retrying up to 5 times, so a losing push rebases onto whatever the other job committed instead of failing outright — general, evergreen fix independent of scheduling. Also retimed update-iss-today.yml to "7 12 * * *" so the two schedules never technically coincide in the first place. No deploy step applies — GitHub Actions workflow files take effect on merge to main, no Worker/Pages deploy involved. |
| F14 | Per-user direct SATCAT fetch | **Fixed and live** | PR #100, merged | Worker gets a new `/satcat?id=` route (`fetchSatcat()`, `SATCAT_TTL` = 7 days — this data is effectively permanent once an object is catalogued) following the exact `cached()`/per-key pattern `/passes` already uses. `enrichSatcat()` in `apps/web/src/ui/info.js` now fetches `${WORKER_BASE}/satcat?id=` instead of `celestrak.org` directly; response shape changed from CelesTrak's raw array to a single record-or-null (client logic simplified accordingly, no behavior change). **Worker deploy attempted 2026-07-17, blocked** — see change log; live `/satcat?id=25544` still 404s, confirming this is genuinely not live yet, not just an unconfirmed guess. |
| F15 | Worker logs unrounded coordinates | **N/A — feature removed** | `claude/remove-pass-alerts-toggle-j3txc6` | Only ever applied to `/passes`'s lat/lng handling, which no longer exists as of 2026-07-28. |
| F16 | Privacy policy / iOS permission-string inaccuracies | **Fixed and live** | PR #72, merged; in TestFlight build | Fixed 2026-07-13, confirmed live in TestFlight the same day. **2026-07-28:** the location half of this finding is now moot — `NSLocationWhenInUseUsageDescription` and privacy.html's Location section were removed outright along with the rest of Pass Alerts, rather than kept accurate. **2026-07-29:** the location half is live again, by design, for What's Overhead — both the permission string and the privacy Location section were rewritten from scratch rather than restored from git. The old copy said coordinates were "sent to our server… to generate pass predictions", which would now be false: the overhead sweep is pure client-side geometry and the coordinates never leave the device. This finding's standing requirement — that the two stay accurate to what the app actually does — is therefore met, and is worth re-checking on any future change to how location is used. **2026-07-30:** the other half of this finding's namesake — the "Fonts" paragraph naming Google Fonts as a third party receiving visitor IPs — is now moot too: fonts are self-hosted (Design item 13), so the paragraph was removed rather than kept accurate. |
| F17 | `/passes` unauthenticated abuse risk | **N/A — feature removed** | `claude/remove-pass-alerts-toggle-j3txc6` | The `/passes` endpoint no longer exists as of 2026-07-28, closing this by deletion rather than by fix. |
| F18 | Mobile rendering hotspots | Not started | — | |
| — | Four non-station objects in `STATION_CORE_IDS`; one live in production as a "station" | **Fixed and live on web** (not yet in an iOS build) | this PR | Not one of the original 37 findings — found 2026-08-17 by diffing the live Worker's `/tle` station list against what an ISS/CSS module list should contain. `STATION_CORE_IDS` held **27386, 28654, 37224 and 37820**, none of which are station modules. Resolved every ID through the live `/satcat` endpoint rather than trusting the list: **27386 = ENVISAT** (ESA Earth-observation, derelict since 2012), **28654 = NOAA 18** (weather satellite), **37224 = O/OREOS (USA 219)** (a NASA 3U cubesat), **37820 = TIANGONG-1** (decayed 2018-04-02). They look like mistaken guesses at the IDs for Unity/Harmony/Tranquility. O/OREOS was **live in production classified `stations`** — a visible-by-default category — so a 3U cubesat rendered as a space station on the globe and in the Orbit Classes legend. The other three are absent from the fetched CelesTrak groups today, so they were latent rather than visible, but each would have promoted itself the moment it appeared in a feed. **This is the row the 2026-07-16 audit called "harmless"** (it noted 37224 "collides harmlessly with an unrelated classified satellite's NORAD ID" and left it) — it was not harmless, which is exactly the trap CLAUDE.md warns about when it says a Low label doesn't predict real-world impact until it's checked against actual catalog data. No replacement IDs were added: Harmony (Node 2) and Tranquility (Node 3) were Shuttle-delivered and have no catalog entry of their own — verified by resolving the plausible candidates, 32500 and 36395, which turn out to be KIZUNA (WINDS) and SDO. Verified per Critical Rule #6 by running `categorize()` over the real bundled catalog: stations goes 10 → 9 (exactly the six ISS + three CSS modules) and O/OREOS falls to `classified`, correct for a USA-designated payload. Also verified in a real Chromium session against the built app with O/OREOS **still delivered tagged `cat:"stations"`** — it renders as CLASSIFIED anyway, confirming ingest()'s re-classification means the fix does not wait on a Worker deploy. New regression tests pin all four bad IDs out of the set by ID (not by count, so a genuine future module can still be added). |
| — | Fabricated ISS-module descriptions on three unrelated satellites | **Fixed and live on web** (not yet in an iOS build) | this PR | Same root cause as the row above and the more serious half of it. The same four wrong IDs were also hardcoded in `apps/web/src/ui/describe.js`'s hero-description list, so three unrelated satellites asserted, in curated first-person copy, that they were ISS modules: ENVISAT returned *"Node 1 — the first US-built ISS module… Unity connects the Russian and American segments"*, NOAA 18 returned *"Node 2 — the primary docking hub for visiting spacecraft including Dragon and HTV"*, and O/OREOS returned *"Node 3 — houses the life support systems… Also home to the Cupola"*. O/OREOS was reachable in production, and combined with the misclassification above it presented as a station, with an ISS module's description. This is the same class as F2 (fabricated expedition data), which was rated Critical/High. The Unity copy was re-keyed to **25575**, Unity's real ID, where it is accurate (its "launched December 1998" matches SATCAT's 1998-12-04); the Node 2 and Node 3 entries were deleted rather than re-keyed, since neither module has an object to attach to. Confirmed in-browser that ISS (UNITY) now carries the Node 1 text and O/OREOS carries none of it. |
| — | `ISS_TODAY_IDS` carried the same four wrong IDs | **Fixed and live on web** (not yet in an iOS build) | this PR | Third copy of the bad ID list, in `apps/web/src/ui/crew.js`. Currently inert — `fetchAndRenderCrew()` gates on `s.id === "25544"` before the set is consulted, which is finding **F23 (dead "Today aboard" per-module config)**, still open. Corrected to the six real ISS module IDs anyway, because the set is a loaded gun pointed at F23: whoever fixes that gate would have made ENVISAT, NOAA 18, O/OREOS and the de-orbited Tiangong-1 start serving "Today aboard the ISS". F23 itself is unchanged and still not started. |
| — | Object-count convention had no enforcement | **Fixed and live on web** | this PR | CLAUDE.md's OBJECT COUNT convention lists four hand-written surfaces that must be updated together whenever the derived figure moves, but nothing checked it, and the figure moves **unattended** — the daily TLE-refresh workflow commits a new `satellites.json`, which rebuilds the splash from a new derived count while README, manifest.json and the App Store listing copy keep the old one. The catalog was measured at **18,997** during this audit: three objects short of flipping the splash to "19,000+" while `store-metadata.md` — the source text for the App Store description — still said 18,000. New `apps/web/test/object-count-sync.test.js` asserts the invariant rather than a fixed number: every rounded-thousands figure in those files must equal what the catalog currently derives. Confirmed it fails on drift by simulating the crossing (catches all five mentions) and that it ignores unrelated figures like "200,000". |

## Bugs — Low (F19–F37)

| # | Finding | Status | PR / Branch | Notes |
|---|---|---|---|---|
| F19 | Every data commit triggers full Pages deploy | Not started | — | |
| F20 | Freshness signal writes to hidden elements | Not started | — | Same fix as Design item 1 |
| F21 | Desktop search keyboard inert | Not started | — | |
| F22 | enrichSatcat never retries after failure | Not started | — | |
| F23 | Dead "Today aboard" per-module config | Not started | — | |
| F24 | Double-caching understates real staleness | Not started | — | |
| F25 | Edge TTLs tighter than source cadence | Not started | — | |
| F26 | NEO catalog frozen, no refresh path | Not started | — | |
| F27 | Worker `/tle` cache stampede | Not started | — | |
| F28 | Service worker rough edges | Not started | — | |
| F29 | `fetchLive` no in-flight guard | Not started | — | Must ship alongside any periodic-refresh work (§4.1 of audit). **Note added 2026-07-13:** F5's async-ingest fix (PR #80) slightly widens the window where two overlapping live syncs could interleave, since ingest() no longer runs as one uninterrupted block. Not a live risk today since nothing triggers periodic syncs yet — but this makes F29 more important to actually do once periodic refresh is built, not just nice-to-have. |
| F30 | GPU/asset misc | Not started | — | |
| F31 | Stale hotlist facts stated as "now" | Not started | — | |
| F32 | Time-machine position tearing | Not started | — | |
| F33 | passAlerts non-atomic cancel+reschedule | **N/A — feature removed** | `claude/remove-pass-alerts-toggle-j3txc6` | passAlerts.js (and the cancel/reschedule logic it described) was deleted outright as of 2026-07-28. |
| F34 | Project-doc drift (CLAUDE.md/ARCHITECTURE.md) | **Fully addressed** | — | The specific Worker-classification contradiction was resolved 2026-07-13 (audit corrected). The /passes-endpoint-missing-from-CLAUDE.md part is now moot rather than fixed — CLAUDE.md's architecture section was updated 2026-07-28 to drop `/passes` (removed entirely) and, while there, to correct a separate long-standing gap: it had never listed `/satcat` (added by F14) either. |
| F35 | Capsule phase can flap | Not started | — | |
| F36 | Hyphen-bounded name regexes mislabel 61% of catalog | **Fixed and live** | PR #90, merged; Worker deployed 2026-07-16 | Full empirical breakdown verified 2026-07-15 against all 18,697 real catalog objects: STARLINK\|ONEWEB 11,439→0 fixed to 11,439 matched; capsule pattern and station pattern also fixed; GALILEO 32→0 fixed to 32 matched (previously zero Galileo satellites got ESA attribution at all); BEIDOU/FENGYUN/etc gap (1,925 of 2,146) fully closed, traced to 1,911 objects sharing the literal name "FENGYUN 1C DEB" (debris from China's 2007 ASAT test), not a bug. Telescope pattern and GPS/NAVSTAR pattern deliberately left untouched — confirmed the two "LEMUR-2-HUBBLE" Spire cubesats still correctly do NOT match "telescope" after the fix (a naive fix would have caused a new false-positive). Reused the already-proven normalizeVehicleName() helper (now exported from packages/catalog) rather than inventing new normalization logic. **Worker deploy confirmed 2026-07-16**: `normalizeVehicleName()` and the fixed capsule/station/OneWeb patterns are present in the deployed source (DEBRIS_NAME_RE/ISS_HARDWARE_RE remain intentionally untouched — outside this finding's scope). |
| F37 | predictPasses window-edge inaccuracies | **N/A — feature removed** | `claude/remove-pass-alerts-toggle-j3txc6` | packages/catalog/src/passes.js (predictPasses itself) was deleted outright as of 2026-07-28. |

## Open Questions (decisions, not fixes — see audit §10)

| Question | Status |
|---|---|
| Monetization shape | Undecided |
| Design direction (Aurora vs. Apple-minimal) | Undecided |
| Units (metric default?) | Undecided — but no longer blocking. As of 2026-07-29 a km/miles toggle exists in Settings (Design item #5), defaulting to imperial to preserve existing behaviour. Flipping the default is now a one-line change to `DEFAULTS.units` in `apps/web/src/settings.js`. |
| Mobile Time Machine — intentional or oversight? | Undecided |
| iOS App Store status | **Answered: preparing the first public App Store submission, as of 2026-08-11** (was internal TestFlight testing since 2026-07-13). Listing copy is finalized in `docs/archive/store-metadata.md`, including the Privacy Policy URL vs. Support URL mapping. Blocked on cutting a new build — see the build tracker at the top of this file. |
| Comfort level with adding accounts | Undecided |
| "COOL SHIT" category label | **Resolved 2026-08-11: the category was removed outright**, not renamed, during the pre-submission audit — from `config.js`'s CATS, `classify.js`'s CATEGORY_IDS, `correctDebrisCat()`'s escape hatch, and `describe.js`'s branch. Verified first that it was latent rather than live: **zero** objects classified `cool` in both the bundled catalog and the live Worker `/tle`, nothing in `classify.js` ever *assigned* it, and `legend.js` skips zero-count categories — so the label rendered nowhere. It was still one hand-curated object away from appearing in the on-screen Orbit Classes legend (visible by default), which would then have contradicted a "no profanity" age-rating answer. Removal is safe by construction: `clouds.js` funnels an unknown cat into `clouds.other` and `classify.js:406` normalizes anything outside CATEGORY_SET to `"other"`. |
| Hotlist — hand-curated vs. derived | Undecided. (2026-07-16: JWST removed from `hotlist.json` — it's not an ID mismatch, JWST's Sun-Earth L2 orbit has no TLE/GP data in CelesTrak under any query, so it can never resolve to a trackable object in this pipeline regardless of hand-curation.) |
| Crew data reliability — Open Notify (`astros.json`) confirmed down in production 2026-07-16; should a committed-JSON fallback (same pattern as `/today`/`/capsules`, refreshed by a scheduled workflow) be built so crew names survive future outages? | Raised 2026-07-16. **Resolved 2026-07-21:** Open Notify has been replaced entirely by Launch Library 2 (see the "Open Notify replaced by Launch Library 2" row above) — LL2 was independently verified this session against NASA, Wikipedia, and Xinhua for both stations, closing both the earlier "down" failure mode (PR #96) and the later "badly stale roster" failure mode (PR #109) at their root, a real data-source fix rather than another workaround. A committed-JSON fallback for LL2 itself is no longer judged necessary given LL2's demonstrated reliability, so it's not being built — revisit only if LL2 itself proves unreliable in practice. **One known residual limitation, not a regression:** LL2's per-station active-expedition data can lag by a few days on a brand-new arrival during a handover overlap — confirmed directly: it was still missing the just-docked Soyuz MS-29 crew as of 2026-07-21. The new `possiblyIncomplete` supplementary check exists specifically to surface this honestly (a small "There may be additional crew not reflected yet" caveat) rather than silently show an incomplete roster. |
| Share card on iOS — should the Capacitor wrapper get a real native share sheet (`@capacitor/share` + `@capacitor/filesystem`) instead of the current save-image fallback? | **Raised 2026-07-31** with the Share feature (PR below). The shipped version is deliberately dependency-free: it uses the Web Share API where the platform supports files (iOS Safari, most Android) and falls back to a normal download elsewhere. Inside WKWebView neither is dependable — `navigator.share` with `files` is inconsistently exposed and `<a download>` is ignored outright — so the wrapper instead gets a full-screen preview with press-and-hold-to-save guidance, chosen because a dead download link there fails **silently**. Upgrading to the native plugins would give a true share sheet straight to Instagram/X/Messages, but it adds native dependencies (Podfile + `npx cap sync ios`) whose only meaningful verification is a real TestFlight build — **it should not be attempted blind from a cloud session**, which is exactly why it wasn't bundled into the original PR. Do it as its own change, paired with a build. |
| Pass alerts feature — keep, remove entirely, or replace with a link to NASA's Spot the Station | **Raised 2026-07-13. Real reason:** Ian doesn't want Orbital Traffic's identity to become centered on/associated with pass alerts — not primarily a NASA-redundancy or maintenance-burden argument, though those are true supporting points. **Resolved 2026-07-28: removed entirely**, on branch `claude/remove-pass-alerts-toggle-j3txc6`, ahead of the next TestFlight build. Removal matched the scope mapped here exactly: apps/web/src/ui/alerts.js, apps/web/src/native/passAlerts.js, packages/catalog/src/passes.js + its tests (+ the re-export in packages/catalog/src/index.js), the Worker's /passes endpoint + its tests (worker/src/index.js's handlePasses/buildPasses/fetchIssTle/PASSES_TTL and the now-unused predictPasses/noradId imports), the toggle markup in index.html, the #alerts/.alerts-row CSS block plus three now-dangling comment references to it in app.css, the two import+call sites in main.js, the Location section of privacy.html (replaced with a "No location access of any kind" bullet), the NSLocationWhenInUseUsageDescription key in Info.plist, and the @capacitor/local-notifications dependency (removed from apps/web/package.json, package-lock.json regenerated, `npx cap sync ios` regenerated CapApp-SPM/Package.swift to drop the CapacitorLocalNotifications product). This closes F7, F15, F17, F33, F37, and the location half of F16 by deletion — see those rows. Needs to ride the next Worker deploy (touches worker/** — see Critical Rule #1) once merged, and should be folded into the next TestFlight build per Ian's request. |

---

## Change log
*(newest first — brief, one line per sync)*

- 2026-08-20 — New feature on branch `feat/today-in-space-events` (PR #179, open for review): **"Today in Space"**, an automated event feed HUD card (collapsed by default, top of the left stack) surfacing docking/undocking, new launches, re-entries/decays, and crew changes without hand-curation — closing the gap where a story like LINK's only became visible because a human happened to notice it. Investigated and confirmed with Ian before building, per the task spec's explicit request: where the diff logic should live, git-history vs. persisted-snapshot per event type, risk to existing scheduled jobs, and the icon/color plan against real design tokens. Landed as: (1) no new state for docking/undocking — it already existed as capsule-status.json's `events` log (`advanceCapsuleLog()`), just newly read by the composed feed; (2) launches/re-entries diffed in `fetch-tles.mjs` itself against the pre-overwrite catalog already on disk (no git-log lookup needed), grouped by shared TLE international-designator prefix so a multi-satellite batch is one event, with capsule-tracked vehicles excluded on both sides to avoid double-reporting a Dragon splashdown as both "LANDED" and (wrongly) "DEORBITED"; (3) crew changes newly diffed in `update-iss-today.mjs` via a best-effort fetch of the Worker's own public `/crew` route (never LL2 directly — its API key is a Worker secret GitHub Actions can't see), persisted into `iss-today.json` alongside the existing NASA-blog headlines without blocking that write on a failed crew check. All three sources stay independently owned/committed (never one shared file three cron jobs write to, which would reintroduce the concurrent-commit collision class F13 fixed); a new Worker route, `/events` (`buildEvents()`, 10-min TTL), composes and time-windows them (48h) at request time, the same "committed JSON, no live upstream computation" shape `/today`/`/capsules` already use. Found and fixed one latent bug along the way: `update-iss-today.mjs` had no import guard, so importing it for testing also kicked off a live `main()` run (its two sibling scripts both already had this guard). `refresh-tle-data.yml` also gained the rebase-retry push loop its siblings already had, now that it commits two files instead of one. Event colors reuse existing CATS tokens (docking=capsules teal, launch=communications orange, reentry=debris grey, crew=the violet `capsule-status.js`'s `.crew-today-dot` already used) rather than a new palette, confirmed with Ian rather than guessed; glyphs (⬡▲▼◆) follow the app's one existing icon-on-a-swatch precedent (the `★` favorites marker) since there's no SVG icon system yet (Design item #14 is still unstarted). Verified against realistic simulated day-over-day data in a real Chromium session (desktop 1440×900 and mobile 390×844, per this project's mobile-first instruction): all four event types render with correct copy/color/glyph/relative-timestamp, tap-to-select works for docking (routes to the real object) and crew (routes to the station), non-selectable launch/reentry rows are visibly inert, and the zero-events empty state shows a calm explicit message rather than looking broken. 533 tests (40 new: `packages/catalog`'s `events.js`/`tle.js` additions, `tools/`'s `fetch-tles.mjs`/new `update-iss-today.mjs` coverage, the Worker's new `/events` route, and the new UI module), lint and build all green. Needs a Worker deploy (touches `worker/**`/`packages/catalog/**`) once merged, per Critical Rule #1.

- 2026-08-17 — **Second pre-submission App Store audit.** Gates first: 476 tests, lint and a production build all green, and all six Worker endpoints re-verified live. The real find came from diffing the live `/tle` station list against what it should contain: `STATION_CORE_IDS` held four objects that are not station modules (ENVISAT, NOAA 18, O/OREOS, the de-orbited Tiangong-1), the same four IDs were hardcoded in `describe.js` as ISS-module hero descriptions, and a third copy sat in `crew.js`'s `ISS_TODAY_IDS`. O/OREOS — a 3U cubesat — was live in production rendering as a space station and describing itself as ISS Node 3 with the Cupola. See the four new rows in the Medium table for the full account, including why no replacement IDs were added and how each was verified against SATCAT and the real catalog rather than assumed. Also: replaced the stock Capacitor launch screen (Design item 18) with generated artwork on the app's own background, with the aspect-fill safe zone now enforced by the generator instead of eyeballed; corrected the iOS location permission string, which called the app "OrbitalTraffic"; refreshed the stale curated-object figures in `store-metadata.md`; and corrected CLAUDE.md's claim that every upload still needs a manual export-compliance answer (resolved 2026-08-11 by the `ITSAppUsesNonExemptEncryption` key). **A new iOS build is still the blocker for submission** — none of this, nor anything merged since 2026-07-13, is on a device.

- 2026-08-11 — **Follow-up to the audit PR (#165): both pre-existing photo-matching issues it flagged are now fixed.** (1) `figures.js`'s `photoKey()` matched `/ HUBBLE | HST /` in addition to NORAD 20580. Space-bounding correctly missed Spire's "LEMUR-2-HUBBLE-4"/"-5" cubesats, but it still matched the two real catalog objects literally named **"HUBBLE 6"** and **"HUBBLE 7"** (both `cat:"other"`) — so two unrelated satellites were showing an unmistakable photo of the actual Hubble Space Telescope, made more conspicuous by #165 replacing that photo with a crisp NASA original. The name fallback is now gone entirely: 20580 is the only object that should ever get this photo, so matching by ID alone is both narrower and simpler. **`describe.js`'s telescope classifier — the pattern CLAUDE.md's exception actually protects — is untouched**; HUBBLE 6/7 still classify as "telescope" there and now land on the `science_generic` pool, which is wrong-ish but no longer claims to be Hubble itself. (2) `describe.js`'s station pattern listed ZARYA/TIANHE/TIANGONG/WENTIAN/MENGTIAN but not **ZVEZDA/UNITY/DESTINY/POISK**, so four permanent ISS modules fell to the "generic" bucket and drew a NanoRacks CubeSat photo despite carrying `cat:"stations"`. All four added, `\b`-bounded and matched against `normalizeVehicleName()` output. False-positive risk checked against the real catalog rather than assumed, since UNITY and DESTINY are ordinary words: each matches exactly one object, all four already `cat:"stations"`, and the catalog's unrelated "HUNITY" correctly does not match (no word boundary inside it). Verified in real Chromium against the built app: HST (20580) still gets the Hubble photo, HUBBLE 6/7 no longer do, LEMUR-2-HUBBLE-4 remains unmatched, and all four modules now draw from `station_generic`. 473 tests (6 new regression tests covering both fixes plus the HUNITY guard), lint and build green. **One cosmetic wrinkle worth knowing:** `station_generic` is a 3-photo pool indexed by `hashStr(id) % 3`, and POISK — a Russian ISS module — happens to hash onto the China Manned Space Engineering Office's Tiangong photo, with a poor crop. Still a strict improvement over the CubeSat photo it drew before, and any station-classified object could always have landed there, but if it grates, the fix is to route ISS modules to the existing dedicated `iss` key rather than the generic pool.
- 2026-08-11 — **Pre-submission App Store audit.** Full codebase/asset/metadata review ahead of the first public submission. **Photo provenance was the one real legal exposure and is now closed:** 15 of 35 bundled images carried the credit `"Source unconfirmed — pre-existing image"` — and `figures.js` renders that credit into the visible on-card caption, so `iss.jpg`, the first thing a reviewer taps, was captioned "Source unconfirmed". Eight were replaced with verified NASA/JPL public-domain imagery sourced through the NASA Image and Video Library API (each asset's metadata checked for a `copyright` field before download; all eight came back clean): ISS → STS-132 flyaround, Hubble → STS-82 flyaround, JWST → mirror reveal, Dragon → Endurance/Crew-7 approach, Soyuz → MS-28 docked, Cygnus → Cygnus XL approach, Phaethon → Arecibo radar, Toutatis → Goldstone radar. Seven were dropped outright, with their files deleted: Geographos and 1862 Apollo (no NASA equivalent exists — searched and confirmed, and the Apollo-*program* photos the search surfaces would be badly wrong for asteroid 1862), plus the first entry of each of the five `_generic` pools, each of which retains three properly-credited NASA/CC entries. Note this reshuffles which pool photo a given object shows, since `figureHTML()` indexes by `hashStr(id) % pool.length` and the pools went 4 → 3. **`"cool"`/"COOL SHIT" removed outright** — see the Open Question row for the full reasoning and the safety argument. **`MARKETING_VERSION` 1.0 → 2.0.0**, resolving a mismatch where an App Store "1.0" install would render "Orbital Traffic v2.0.0" in Settings → About (the web app reads `apps/web/package.json`, which is 2.0.0, as does the CHANGELOG). **`esc()` added to `figures.js`'s `photo.path`/`photo.credit`** — the custom `no-unescaped-innerhtml` rule structurally cannot see this spot, since its documented scope is a direct `X.innerHTML = <TemplateLiteral>` and this value returns from a function call first. Not exploitable (same-origin build asset), fixed per CLAUDE.md Rule #7 rather than left to the data file staying well-behaved. **Listing copy rewritten** in `docs/archive/store-metadata.md`, which now also records the Privacy Policy URL vs. Support URL mapping: one factual error (Cygnus described as a crewed capsule — it is uncrewed cargo, and `CARGO_VEHICLE_PATTERNS` says so), three overclaims softened against what the app actually does, near-Earth asteroids and Time Machine added, the missing subtitle supplied, and NASA/SpaceX dropped from the keywords as third-party trademarks. **This file resynced:** 15 rows still said "Fixed, not deployed" and 18 PR references still said "open" — all had merged, and all six Worker endpoints were re-verified live against production, including `/satcat`, which was still 404ing at the last sync. Verified in real Chromium at 430×932 against the built app with a stubbed Worker: all 28 photo entries load with correct credits, the legend renders 13 categories with zero profanity rows, and no page errors. 467 tests, lint and build all green. **Two pre-existing issues found and deliberately NOT fixed here** (out of the audit's scope, both predate it): `figures.js`'s space-bounded `/ HUBBLE | HST /` pattern matches the catalog objects literally named "HUBBLE 6"/"HUBBLE 7", which now show a crisp real Hubble photo — the CLAUDE.md exception about this pattern covers the LEMUR-2-HUBBLE cubesats (which it correctly still does *not* match) but not this case; and `describe.js`'s `classify()` station pattern lists ZARYA/TIANHE/TIANGONG/WENTIAN/MENGTIAN but not ZVEZDA/UNITY/DESTINY/POISK, so those ISS modules fall to the `satellite_generic` pool and show a CubeSat-deployment photo despite being `cat:"stations"`.
- 2026-07-31 — **App icon redesigned and promoted to the app's full visual identity** (PR #156, `claude/app-store-icon-redesign-aiy44q`, merged). Ian supplied new satellite artwork whose 1024x1024 export App Store Connect rejected; root cause was an alpha channel, which the store rejects even when every pixel is fully opaque. Rather than hand-fix one PNG, all 19 rasters (14 PWA sizes, the three aliases, the maskable variant, the iOS AppIcon) now generate from a single committed vector, `tools/assets/app-icon.svg`, via `npm run icons:generate` — the generator verifies every PNG it writes (dimensions, 8-bit colour type 2, no `tRNS`, no interlacing) and refuses to exit clean otherwise. Playwright is deliberately NOT a dependency: icons are committed artifacts and the script runs by hand, so CI and a normal `npm install` need no browser download. **Two pre-existing bugs found and fixed along the way:** (1) `icon-maskable-512.png` was byte-identical to `icon-512.png`, so it had no safe-zone padding whatsoever and Android would have cropped into the artwork — the maskable variant now scales the spacecraft inside the centred 80% circle, produced by rewriting the source's `#craft` transform so the two variants cannot drift apart; (2) both inline HUD/splash brand marks in `index.html` declared `<linearGradient x1="4" ... x2="60">` while leaving `gradientUnits` at its default of `objectBoundingBox`, where coordinates are 0-1 fractions — values of 4-60 collapsed the whole shape to the first stop, so the mark had *never* rendered the teal→violet brand gradient the wordmark beside it uses, it just showed flat near-white. Added `gradientUnits="userSpaceOnUse"`. Both marks were also redrawn from the orbit-ring to the satellite silhouette, keeping the app's own gradient so they stay harmonious with the adjacent wordmark. **`manifest.json`'s `theme_color`/`background_color` were reviewed and deliberately left unchanged** — an earlier note on PR #156 speculated they were leftovers from the old teal orbit-ring mark, and that was wrong: `background_color:#07080f` is exactly `app.css`'s `--bg` (so the PWA splash matches the real UI and doesn't flash on load) and `theme_color:#5eead4` is exactly `--amber`, the app's pervasive accent. Both describe the app, not the old icon, and the new icon's gradient runs the same teal→indigo hue journey anyway. Note this does **not** close Design item #14 ("standardize icons"), which is about in-app UI glyphs, not app/launcher icons. Filed new Design item #18 for the iOS splash placeholder.
- 2026-07-31 — New (non-audit) finding, fix on branch `fix/search-ranking` (open for review): **catalog search returned matches in raw catalog order**, so any object whose name is a substring of a large constellation's was unreachable. `ui/search.js` was a bare `filter(...).slice(0, 40)`; measured against the real 18,755-object catalog, `"link"` matched **10,774 objects and the actual LINK spacecraft (69792) sat at index 10,769**, behind every `STARLINK-*` — past the 40-result cap, so it never appeared at all and the object could only be reached by typing its NORAD id. Found while verifying the share-card work above, which had to search by id for exactly this reason. Fix is ranking only — **matching is unchanged** (same case-insensitive substring on name or id): results are bucketed exact → prefix → substring, with catalog order preserved inside each tier, so queries where everything ties (`"starlink"`, all 10,771 hits prefix matches) come out in exactly the order they did before. Bucketed rather than sorted since there are only three tiers: O(n) instead of O(n log n), and capping each bucket at the result limit also removes the 10,774-element intermediate array the old `filter` built on every keystroke. Measured in-page on the real catalog: **2–3ms per keystroke including DOM render.** Deliberately *not* included: word-boundary as a fourth tier (would rank "CREW DRAGON" above a mid-word hit), and `normalizeVehicleName()`-based matching — both change which objects match or how, beyond the reported bug. Verified in real Chromium against the live catalog: `"LINK"` → LINK first; `"starlink"` → unchanged; `"iss"` → ISS (ZARYA), ISS (NAUKA), then ISS OBJECT debris; `"25544"` → ISS by exact id; `"hubble"` → HUBBLE 6/7 above the LEMUR-2-HUBBLE-* cubesats. 452 tests pass (16 new, including a regression test that reproduces the original burial with the exact match last in catalog order); lint and build clean.
- 2026-07-31 — New feature on branch `feat/share-object-card` (open for review): **shareable object cards**. Tapping "Share Image" on the info card exports a 1080×1350 (4:5) PNG combining a real render of the globe — camera framed on the selected object, its actual current position and orbit path in shot — with the object's name, category, live altitude/speed and curated blurb, plus the brand mark. Client-side only: no Worker route, no new upstream load, no new runtime dependency. **Capture** renders into an offscreen `WebGLRenderTarget` rather than reading back the visible canvas: the renderer is created without `preserveDrawingBuffer` (scene/core.js), so a canvas readback would need a synchronous render→`toDataURL` pair *and* a temporary resize of the canvas the user is looking at. The **existing** renderer is reused rather than a second one being constructed — WebKit caps live WebGL contexts and evicts the oldest under pressure, so a second renderer risks killing the globe's own context on the one platform this has to work on. Nothing taints the canvas: every texture in this scene is generated procedurally onto an offscreen 2D canvas already. Two real image-quality bugs were found and fixed during visual verification, neither of which any test would have caught: (1) point clouds use `sizeAttenuation:false`, i.e. a size fixed in *device pixels*, so rendering into a target larger than the live drawing buffer made every satellite cover proportionally less of the frame — the export came out visibly sparser than the screen; material sizes are now scaled by the same ratio as the render size and restored afterwards. (2) `antialias:true` applies to the default framebuffer only and does nothing for a render target, so the globe's limb came out jagged — the pass supersamples 2× and downscales instead of using MSAA, because the point sprites are alpha-tested (`alphaTest:0.08`) and MSAA barely touches cutout edges. The orbit ring is drawn in the 2D pass, not taken from the scene: WebGL ignores `linewidth`, so the scene ring is always a hairline and would have been near-invisible at 1080px wide; `trail.js` now exposes `orbitRingPoints()` so both trace the same curve, and `setTrailVisible()` so the two don't overlay. Camera framing is analytic rather than reusing the live rig (which is smoothed, so a capture taken right after `frameSelected()` catches it mid-flight); it searches offset pairs for one that keeps the subject inside a safe box and in front of the Earth. A longitude offset alone proved insufficient — a camera near the orbit's own plane sees the ring edge-on however far round it moves, which is why an inclined LEO orbit still exported as a near-straight line — so a **latitude** offset was added, which is what actually opens the ellipse; confirmed against a real Starlink before/after. Text is drawn with the canvas 2D API, not html2canvas (a new dependency that re-implements CSS approximately) or SVG `foreignObject` (unreliable and taint-prone in WebKit). Stats reuse `subpoint()` and the `util/units.js` formatters the info card already uses, so a shared image can't disagree with the panel it came from and the km/miles setting is honoured. **Also in this PR:** a curated `descriptions.json` entry for LINK (69792) — with LINK now classified `science` (PR #151), `describe.js` was falling it through to the generic "A space telescope…" blurb, which would have been visibly wrong on its card; added via `tools/merge-descriptions.mjs` rather than by hand so the file's format and no-overwrite rule are preserved. Verified in real Chromium at 430×932 against ISS, LINK and a Starlink: all three cards inspected as images, both the download path and (stubbed) Web Share path exercised end-to-end. 436 tests pass (33 new across 2 files, covering the framing math and text fitting — jsdom has no canvas 2D, so the drawing itself is verified visually, not by assertion); lint and build clean. **Follow-up tracked** as a new Open Question: a true native iOS share sheet needs `@capacitor/share`/`@capacitor/filesystem` and a TestFlight build to verify, so it was deliberately left out.
- 2026-07-30 — Design item 13 (self-host fonts, PR #143 `claude/self-host-google-fonts-szv1c0`, merged): confirmed Bricolage Grotesque/Oxanium/Orbitron are all SIL OFL 1.1 (license text bundled at `apps/web/public/fonts/OFL-*.txt`), then swapped the `fonts.googleapis.com` `<link>` in `index.html` for local `@font-face` rules in `app.css` serving latin-subset-only woff2 (the app renders no non-ASCII text) at exactly the weights the old Google Fonts URL requested — nothing wider. Carried over the existing `unicode-range` verbatim since `app.css` already documented that Oxanium's range excluding flag-emoji codepoints is load-bearing for the desktop emoji-fallback fix. Closes the font half of F16 too — privacy.html's "Fonts" paragraph (Google receiving visitor IPs) no longer applies and was removed. Verified in a real Chromium session that all three families load from `/fonts/*.woff2` with no `fonts.googleapis.com`/`fonts.gstatic.com` requests, and visually on both a 430×932 and 1440×900 viewport. 326 tests, lint, and build all green.
- 2026-07-29 — Second same-day follow-up on the branch below, per Ian's direct feedback after trying the first pass: (1) **Geostationary demoted out of `OVERHEAD_TIER1_CATS`.** Real-catalog measurement at the time showed why: at several locations (e.g. Dallas, Sydney) geostationary was over 90% of all Tier-1-eligible objects, crowding the curated top-25 with fixed-elevation birds instead of genuinely time-varying traffic — the exact failure mode Tier 1 exists to avoid. It still appears in results, ranked by elevation with Starlink et al. (2) **`OVERHEAD_EXCLUDED_CATS` added** — "other" and "debris" now never appear in `overheadSweep()`'s results at any elevation, a hard exclusion applied the same way the null-satrec NEO skip already was, not a ranking demotion. Re-measured at the corrected threshold across the same seven locations: **41-252 qualifying objects** (down from the earlier 86-277 now that other/debris are gone; still nowhere near the <5 sparse case flagged earlier as worth watching). (3) **Settings' Display → categories-shown-at-load control removed entirely** — not hidden, not deprecated, deleted: `settings.js`'s `displayCategories` field, `defaultDisplayCategories()`, `seedDisplayCategories()`, and its `main.js` boot call are all gone, along with the 14-row toggle list in `ui/settings.js`. It was both the largest single contributor to Ian's "everything runs together" complaint and redundant with the in-scene Orbit Classes legend, which already toggles every category live — a genuine scope cut, not a bug fix. (4) **The rest of Settings redesigned** into per-section cards (`ui/settings.js`'s `card()` + `.set-card` in `app.css`) — an icon and a distinct accent color per section (Privacy=signal blue, Display=amber teal, Data=violet, About=neutral) reusing existing tokens, real card boundaries instead of a single 1px `border-top` between sections, the About links row turned into icon-labeled pill chips, and the data-source credits turned into two-line name/description rows. Verified in Chromium at 390×844 and 1440×900 with the real panel rendered (screenshots taken, not just DOM assertions). 326 tests pass (7 net fewer than the prior entry's 328 — the category-toggle tests were deleted, not just the feature); lint and build clean.
- 2026-07-29 — Two new features on branch `claude/new-session-12sdbb` (open for review): **What's Overhead** and a **Settings panel**. Client-side only — no Worker route, no new upstream load, no change to the ambient propagation loop or the catalog/classification pipeline, so this auto-deploys to web on merge with no Worker step. **What's Overhead**: new `astro/overhead.js` does a one-shot look-angle sweep of the whole in-memory catalog against a fresh location fix (`eciToEcf` + `ecfToLookAngles`, previously unused in this repo; recovered the approach from the deleted `packages/catalog/src/passes.js`). Chunked across animation frames at 2,500/batch — the `ingest.js` pattern — rather than a Web Worker, which is the wrong tool here twice over: `ingest.js` discards `l1`/`l2` once the satrec is built so a worker has no TLEs to receive, and structured-cloning 18,757 satrecs per tap would cost more than the sweep. Measured against the real 18,757-object catalog: **24-173ms per sweep** (cost is independent of the elevation cut — every object still gets propagated and checked). The initial build used the spec's literal 0° elevation cut with a single elevation-only sort; measuring it against the real catalog showed why that was the wrong bar — it returns ~1,100 objects from a typical location, technically "above the horizon" but useless, dominated by whichever Starlink/geostationary satellites happen to be up, so it shipped **same-day corrected** before merge: a 40° `MIN_OVERHEAD_ELEVATION_DEG` curation threshold, plus a two-key `rankOverhead()` sort — Tier 1 (stations/capsules/science/geostationary/communications/classified) always ranks ahead of Tier 2 (everything else), elevation-descending within each tier, so a 45° station outranks an 85° Starlink. Tier 2 is never excluded, only ranked lower. Measured at 40° across seven varied locations (mid-latitude, high-latitude, polar, equatorial): **86-277 qualifying objects, no location came close to an empty/sparse result.** Default view shows the top 25 (was 50) with the honest total in the header, category filter chips, and a "Show all" control that reveals the rest of the same precomputed, already-ranked list — confirmed by test that it triggers no recalculation. Reuses the orphaned `.ohrow` CSS left behind by the Pass Alerts removal. **Settings**: new `settings.js` (one validated `ot-settings` localStorage key), `data/location.js`, `util/units.js`, `util/motion.js`. Closes Design item #5 (see that row). (Display originally also had a categories-shown-at-load toggle list seeding `state.hidden` at boot; removed the same day — see the follow-up entry immediately above.) **Location access returns** for the first time since Pass Alerts, on a different footing: coordinates are read on demand, used entirely on-device, never stored or transmitted — so `NSLocationWhenInUseUsageDescription` is back in Info.plist (new wording; the old string described sending coordinates to a server and would now be false) and privacy.html's Location section is restored, replacing the "No location access of any kind" bullet with "No location data ever leaves your device". This partially reopens the location half of F16 **by design**, with a materially better posture than the version that was deleted. Also corrected while in privacy.html: its third-party list still named Open Notify, replaced by LL2 back on 2026-07-21. One genuine bug found and fixed in the new code during testing: a malformed satrec propagates to a position object full of `NaN` rather than to nothing, and since `NaN` fails every comparison it slipped past the `<= minElevation` horizon filter into the results — `lookAngles()`/`subpoint()` now reject non-finite values. Two overlap bugs found by driving the real app (Chromium, 390×844 and 1440×900) and fixed: the boot hint printed through the panel header, and the FAB/gear floated over the list rows at both breakpoints. 328 tests pass (76 new across 6 files, including the same-day 40°/tiering correction and its own new coverage); lint, build and a real-browser pass all green. **Still needs:** the iOS permission string only takes effect on the next TestFlight build, so the feature is web-only until one is cut.

- 2026-07-28 — Pass Alerts removed entirely (branch `claude/remove-pass-alerts-toggle-j3txc6`, per Ian's decision on the long-open "keep/remove/replace" question, ahead of the next TestFlight build). Deleted apps/web/src/ui/alerts.js, apps/web/src/native/passAlerts.js, packages/catalog/src/passes.js + its tests, and the Worker's `/passes` route (`handlePasses`/`buildPasses`/`fetchIssTle`/`PASSES_TTL`) + its tests. Removed the toggle markup from index.html, its CSS from app.css (plus cleaned up three comment references elsewhere in the file that pointed at the deleted `#alerts` block), and the two import/call sites from main.js. Dropped `@capacitor/local-notifications` from apps/web/package.json (lockfile regenerated) and `NSLocationWhenInUseUsageDescription` from Info.plist — `npx cap sync ios` regenerated CapApp-SPM/Package.swift to drop the CapacitorLocalNotifications SPM product accordingly, so the app no longer requests location access anywhere. privacy.html's Location section was removed and replaced with a "No location access of any kind" bullet. CLAUDE.md's architecture section updated to drop `/passes` from the Worker's endpoint list (and, while there, corrected a separate pre-existing gap: it had never listed `/satcat`, added by F14, either). Closes F7, F15, F17, F33, F37, and the location half of F16 by deletion (see those rows) and resolves the Pass Alerts Open Question. Full web build (`npm run build`) verified green after the changes. Merged with main's `/astronaut` route work below (no real overlap — that route was untouched by this change). Still needs a Worker deploy per Critical Rule #1 once merged, and should be included in the next TestFlight build.
- 2026-07-28 — **Production/main drift found and being closed.** The crew-roster dedupe fix (LL2 lists the same astronaut twice in one expedition — confirmed live 2026-07-24 on ISS Expedition 74, id 732 appearing twice with identical role) was manually deployed to the live Worker on 2026-07-24 via `npx wrangler deploy`, but the commit never reached `main`: PR #128 had already been merged minutes earlier carrying only its docs commit, and the later push landed on a branch whose PR was already closed. Net effect: the live Worker carried dedupe code that `main` did not, so the *next* push to main touching `worker/**` would have auto-deployed a Worker without it and silently reverted the fix in production. Now re-submitted as its own PR off current main. **Lesson worth keeping:** a manual `wrangler deploy` makes a fix live but proves nothing about it being merged — after any manual deploy, verify the commit is actually an ancestor of `main`, not just that production behaves. This is the mirror image of Critical Rule #1's usual failure (merged but never deployed).
- 2026-07-24 — New feature (PR #131, `feat/astronaut-bios`, merged — kept as its own PR, stacked on the dedupe fix in PR #132, so the crew work and this feature review separately), first thing built on the new Space Devs API key: **astronaut profiles in the crew card**. Also closes Design item #17 above. Tapping a roster avatar expands that person's LL2 profile — photo, nationality/agency, bio, and flight/EVA/time-in-space stats — into one shared panel below the avatar grid (one panel rather than one per avatar, so opening a bio never reflows the wrapped avatar rows). New Worker route `/astronaut?id=` returns a *trimmed* projection of LL2's record, not a passthrough (their full record carries nested flight/landing/spacewalk arrays worth tens of KB, all unused); id is validated numeric-only so a caller can't steer the upstream URL path or mint unbounded cache keys. `ASTRONAUT_TTL` is 24h — well short of SATCAT's 7 days because flight/EVA counts do move mid-mission, but long enough to collapse essentially all repeat traffic. `/crew` now also passes each member's astronaut `id` (what the card looks up) and `role` through; `role` was already being read by crew.js's commander highlight (`p.role`) but had never actually been sent, so that highlight had silently been "whoever is listed first" — it now marks the real commander, with the old positional rule kept as the fallback for a roster served from an edge cache filled before roles existed. Profiles are cached client-side for the page session. Verified in a real browser (Playwright, Chromium) at 1440×900 and 390×844 with mocked Worker payloads: expand/collapse/switch, the no-photo and failed-fetch fallbacks, and the mobile bottom-sheet layout all behave, with avatar hit boxes growing 36×46 → 46×54 on mobile. 244 tests pass (13 new jsdom tests for the accordion, 6 new Worker tests); lint and build clean.
- 2026-07-24 — Live `/crew` fetch failure (PR #113 below) resolved. Ian had been seeing "crew names unavailable" for ~3 days — confirmed via a production curl that `/crew` was returning `ok:false` with `sourceStatus` 429 ("Request was throttled") from LL2 on both stations, matching the anonymous-IP-throttling root cause PR #113 diagnosed. The `LL2_API_KEY` code path was already merged and deployed (part of PR #113/#111's Worker deploy); the only missing piece was the secret value itself. Ian created a Space Devs API key and a scoped, short-TTL Cloudflare API token (`Account:Workers Scripts:Edit`, single account, ~1-day expiry); `npx wrangler secret put LL2_API_KEY` was run from `worker/` and the token was deleted immediately after. Verified live: `/crew` now returns `ok:true` with real rosters (11 people across ISS/Tiangong). Noted but not investigated as part of this fix: LL2's own ISS roster currently lists "Andrei Fedyaev" twice and `possiblyIncomplete:true` — that's upstream LL2 data, not a Worker-side bug.
- 2026-07-22 — New (non-audit) finding, fix open for review (PR #113, `fix/ll2-crew-fetch-failure`): the live `/crew` endpoint is failing on every call in production (`ok:false`, empty roster, fresh `fetchedAt` each request) even though the same LL2 URLs return valid data from any non-Workers IP — the failure is specific to Cloudflare Workers egress. Leading suspect: LL2 throttles anonymous traffic at 15 requests/hour/IP (confirmed in The Space Devs' docs) and Workers egress IPs are shared across customers, with PR #96's don't-cache-failures behavior turning every visitor into another hit on the already-throttled shared IP. Fix: (1) `/crew` now surfaces a `sourceStatus` diagnostic (per-station upstream HTTP status + short sanitized LL2 `detail`, only when `ok:false`) so one production curl reveals the refusal reason; (2) both LL2 fetch helpers now send identifying headers (User-Agent + `Accept: application/json` — they were the only upstream fetches sending none); (3) optional `LL2_API_KEY` Worker secret sent as `Authorization: Token <key>` (The Space Devs' documented scheme), no behavior change when absent; (4) failed `/crew` builds negative-cache for 90s (`CREW_FAIL_TTL`) instead of not at all, so a throttled IP can actually recover. Cannot be reproduced or confirmed-fixed from a sandbox (the trigger is Workers egress itself) — after merge + auto-deploy, curl `/crew`: `sourceStatus` 429 → Ian creates a Space Devs API key and runs `npx wrangler secret put LL2_API_KEY` (persists across future deploys per Cloudflare docs); 403/blocked → fall back to a committed-JSON crew file refreshed by a scheduled workflow (same pattern as `/today`/`/capsules`, deliberately not built in this PR).
- 2026-07-21 — Architecture item #4 fixed (not deployed): added `.github/workflows/deploy-worker.yml` (PR #112, `feat/automatic-worker-deploy`, merged), closing the single most recurring failure mode on this project — Worker deploys being manual, separate from merges, and easy to forget (Critical Rule #1). Triggers on push to main touching `worker/**` or `packages/catalog/**` (the two places that can change Worker behavior — `worker/src/index.js` imports `parseTle`/`mergeRecords`/`GROUPS`/`CELESTRAK_BASE`/`FETCH_HEADERS`/`noradId`/`predictPasses` from `@orbital-traffic/catalog`, verified directly against the current file rather than assumed), plus `workflow_dispatch` for on-demand/retry runs. Runs the Worker's own test suite (`npm test -w worker`, confirmed empirically to scope to only `worker/test/worker.test.js` — no shared `vitest.config` exists, so vitest's default root-relative discovery naturally isolates it from `apps/web`'s and `packages/catalog`'s test suites without any extra config) as a pre-deploy gate, then deploys via `cloudflare/wrangler-action@v3`. Non-cancelling `concurrency` group prevents overlapping deploy runs. CLAUDE.md's Critical Rule #1 and DEPLOY COMMANDS section updated in the same change to describe the new automatic path while keeping the manual `npx wrangler deploy` sequence documented for local/pre-merge testing. **Does not retroactively deploy anything already merged** — PRs #107, #108, #109's Worker-relevant parts, and #111 (the LL2 crew-source swap immediately below) are all still pending an actual Worker deploy as of this writing.
- 2026-07-21 — Open Notify replaced entirely by Launch Library 2 (LL2), the real fix for the roster-staleness finding below. worker/src/index.js's buildCrew() now fetches both stations' real active-expedition crew from LL2's production `/spacestation/{id}/` endpoint (ISS=4, Tiangong=18 — verified directly against real crew rosters via NASA/Wikipedia/Xinhua). Partial-station-failure behavior preserved (ok:true if either station succeeds). Added a new supplementary `possiblyIncomplete` check comparing placed headcount against LL2's own in-space astronaut count — confirmed via a real (non-mocked) check that LL2's per-station data currently lags the just-docked Soyuz MS-29 crew by several days, exactly the scenario this check exists to surface honestly. Confirmed no rate-limit headers on any of the three LL2 endpoints hit per cache-fill. The PR #109 plausibility stopgap stays in place as a harmless generic backstop but is no longer primary. Needs a Worker deploy per Critical Rule #1 to go live.
- 2026-07-21 — New (non-audit) finding: Open Notify's crew roster was found to be serving names roughly 18 months stale (verified directly against NASA/Wikipedia), while responding with plausible-looking headcounts — a failure mode neither F1b's ok:false handling nor F3's stale-selection guard could catch. Added a partial stopgap: crew.js compares Open Notify's headcount against seats actually docked at the station right now (state.capsulesData + a new CREW_SEATS_BY_FAMILY table in classify.js), flagging only gross count mismatches with a small non-blocking caveat line. Explicitly does not and cannot catch the actual incident (wrong names, plausible count) — replacing the data source is tracked separately as the real fix. Client-side only, auto-deploys on merge, no Worker step needed.
- 2026-07-20 — New (non-audit) regression found and fixed: F1b. Commit e0780e9's F1 fix (2026-07-13) switched CREW_URL from http:// to https://, believing Open Notify supported HTTPS — it does not (verified directly: HTTPS connection refused/reset, HTTP returns 200 with valid data). This silently broke the live `/crew` endpoint for about a week (`ok:false`, empty roster for every visitor), undetected until now. Reverted CREW_URL to http:// with a warning comment against switching back without testing first; added a Known Bugs entry to CLAUDE.md. F1's actual XSS fix (esc()-escaping in crew.js) is untouched and unaffected. Needs a Worker deploy per Critical Rule #1 to go live.
- 2026-07-17 — PR #100 merged (F14). Immediately attempted the required Worker deploy per Critical Rule #1: `git pull origin main` + root `npm install` succeeded, but `cd worker && npx wrangler deploy` was blocked — `npx wrangler whoami` reports "You are not authenticated" and no `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` are set in this sandboxed session's environment. Same failure mode documented for the PR #96/#90 deploy attempts. Confirmed via the Cloudflare MCP connector's `workers_list` that the live Worker's `modified_on` is still `2026-07-16T17:47:00Z` (unchanged since the last real deploy), and a live curl of `/satcat?id=25544` 404s — so both F14 (this PR) and the still-pending PR #96 crew-fetch hardening (`ok:false`/non-caching-of-failures) remain genuinely not live. Still needs a manual `npx wrangler deploy` run by Ian locally, or Cloudflare credentials provided to a session.
- 2026-07-17 — F14 fixed (PR #100, `claude/satcat-via-worker-uw51zr`, merged, not merged): added a `/satcat?id=` Worker route so SATCAT lookups (launch date, owner, launch site) are proxied and edge-cached server-side instead of every visitor's browser hitting `celestrak.org` directly. 7-day TTL since this metadata is effectively permanent per object. Client's `enrichSatcat()` updated to match. Full test suite (161 tests), lint, build, and a `wrangler deploy --dry-run` all pass. Not deployed to the live Worker yet — needs `npx wrangler deploy` after merge per Critical Rule #1.
- 2026-07-16 — PR #96 merged (crew-fetch hardening in `worker/src/index.js` + the other 6 UI/data fixes below). Worker deploy attempted immediately after merge per Critical Rule #1, but blocked: this sandboxed session has no `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` and no authenticated `wrangler login` session (`npx wrangler whoami` confirmed "You are not authenticated"). Same failure mode as the prior session's PR #90-era deploy attempt. **The crew fetch's `ok:false` shape and non-caching-of-failures fix is NOT live yet** — still pending a manual `npx wrangler deploy` run by Ian locally, or credentials provided to a session. Everything else in this PR (NEO search, JWST hotlist removal, Globe Style/legend/privacy-link/heading UI fixes) is client-side only and already live via the automatic GitHub Pages deploy.
- 2026-07-16 — Six Ian-reported bugs fixed in one pass (branch `claude/app-issues-iss-neos-ui-eafvyv`), two with root causes different from what was assumed:
  - **Crew names not displaying**: verified against the *live* Worker (not just this session's sandbox) that Open Notify's `astros.json` is genuinely unreachable right now — both `buildCrew()` and `fetchAndRenderCrew()` were silently collapsing that failure into the same shape as "really 0 crew," and the Worker cached the failure for the full 1-hour CREW_TTL. Fixed to surface an honest "temporarily unavailable" state and skip caching failed fetches (`cached()` now takes a `shouldCache` predicate). Does **not** restore real names until Open Notify itself recovers — nothing can fabricate real astronaut data. A committed-JSON fallback (same pattern as `/today`/`/capsules`) is a real follow-up, not attempted this session since there was no live data available to seed or validate it against.
  - **JWST hotlist entry**: confirmed via CelesTrak's own satcat (`"ORBIT_CENTER":"EL2"`) that NORAD ID 50463 is correct for JWST, but CelesTrak publishes zero TLE/GP data for it under any query (standard or supplemental) because JWST's L2 orbit can't be modeled by SGP4 — no ID fixes this. Per Ian's decision, removed JWST from `hotlist.json` rather than building permanently-disabled UI for an object that can never resolve to a trackable record.
  - NEO search: `search.js` only ever filtered `state.sats`; NEOs live in `neoSats` (scene/neos.js) and were never included. Fixed by concatenating `neoSats` into the searched collection — selection already worked via scene picking, so no other changes needed.
  - Orbit Classes legend: header click only ever expanded, never collapsed, once auto-collapsed on object selection. Made bidirectional (same pattern as the existing Today-card toggle); auto-collapse-on-select behavior unchanged.
  - Privacy footer link: `#meta{pointer-events:none}` cascaded to the anchor since nothing re-enabled it and the generic `.hud>*` rule lost the specificity fight against the `#meta` ID selector. Added `#attr a{pointer-events:auto}`.
  - Globe Style card: moved from the bottom-left stack to sit directly under the top-right clock card, and made visibly more compact (tighter padding/font-size) per Ian's request.
  - Also renamed "Popular Objects Today" to "Popular Objects" (Ian's request, no bug).
- 2026-07-16 — Cloudflare Worker deployed (via the new `.claude/skills/deploy-worker` skill, run by Ian locally after this sandboxed session's `wrangler deploy` was blocked by missing credentials + network policy). Confirmed via the Cloudflare MCP connector's live worker-code read (not just an endpoint curl) that all five pending items are now live: F9 (OneWeb→oneweb), F10 (Kuiper→kuiper), F36 (tokenization fix), the capsules-category-split (PR #93), and the ISS Unity/Zvezda/Destiny station-allowlist fix (PR #94). Live `modified_on` timestamp jumped from 2026-07-13 to 2026-07-16T17:47:00Z, matching the deploy.
- 2026-07-16 — New (non-audit) finding fixed: ISS Unity/Zvezda/Destiny were missing from STATION_CORE_IDS, discovered during PR #93's verification. Fixing it required more than adding the three IDs — correctOtherCat() needed its own STATION_CORE_IDS promotion check, since these modules arrive tagged "other" in the real feed rather than "stations". Verified against real catalog data.
- 2026-07-15 — Resynced from Ian's working copy, which had drifted from the repo since 2026-07-13 (F1–F8, F11, F11b, F12, F16, F36, the iOS build-batch tracker, and the pass-alerts open question had never been committed back). Combined with this session's own F9/F10 fix: both now "Fixed, not deployed" (PR #92), along with Design item 9 (Starlink/Kuiper duplicate color, same root-cause change as F10). Verified against real catalog data: 651/651 OneWeb and 393/393 Kuiper objects resolve correctly. Note: the working copy's F9/F10/Design-9 rows still said "Not started" at the time it was captured — updated here to reflect this PR instead of taken verbatim, since that's what the rest of its own content (and Ian's own description of it) clearly intended.
- 2026-07-15 — CLAUDE.md updated (PR #91, merged) with new shared-utility conventions (esc(), normalizeVehicleName()), the missing /passes endpoint, corrected iOS build description, and two new Known Bugs entries.
- 2026-07-13 — F12 fully closed via PR #83 + #84, including a genuine additional gap (info.js's ownerName chips row) that the new custom ESLint rule surfaced on its own. That rule is now a permanent safety net, not just a one-time fix — includes a same-scope const-resolver so it checks inside previously-blanket-exempted multi-line templates too.
- 2026-07-13 — F8 merged (PR #82) — fallback data path now uses the same priority-preserving merge as the Worker.
- 2026-07-13 — F11b verified as already fixed (pre-existing, predates this conversation) — traced the epoch-guard prune mechanism and its dedicated test coverage, confirmed it fully addresses the finding.
- 2026-07-13 — F4, F5, F6 all merged (F4 fixed the GitHub-connector PR-opening snag along the way). Holding off on a new TestFlight build for now, per Ian — queue sitting at 3/3-5 until he's ready.
- 2026-07-13 — F16, F7, F3, F1, F2 all confirmed fixed and live via TestFlight build (after PR #78 fixed a build-number auto-increment bug that was blocking the upload). Build queue cleared.
- 2026-07-13 — Initial tracker created. Seeded F16 as "Fixed, not deployed" (PR #72), the Worker-classification Architecture item as N/A (audit corrected), F34 as partially addressed, and the iOS App Store open question as answered (internal testing).
